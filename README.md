# marquee_training

Marquee Training

This repository is configured for automatic GitHub pushes.

## Auto-push

- Manual commits push automatically through the tracked git hook.
- To auto-commit and auto-push after file edits, run `scripts/auto-push.ps1` in PowerShell.

Remote: https://github.com/orpheusdark/marquee_training.git
